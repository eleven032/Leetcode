盛最多水的容器：
给定 n 个非负整数 a1，a2，...，an，每个数代表坐标中的一个点 (i, ai) 。画 n 条垂直线，使得垂直线 i 的两个端点分别为 (i, ai) 和 (i, 0)。

找出其中的两条线，使得它们与 x 轴共同构成的容器可以容纳最多的水。

思路：
装水多少，取决于短的那一边的大小。

先将底边设置为最大长度，然后分别从数组的两端向中间移动，每次移动较小的那一边，得到最小值